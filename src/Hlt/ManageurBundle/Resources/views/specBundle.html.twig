{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div id="header" class="jumbotron">
            <h1>Ma plateforme de Gestion De Repas</h1>
            <p>
                Vous pouvez creer des produits, des repas et les manipuler grace a l'ensemble des operations du CRUD (lister, editer, ajouter, voir, supprimer )
            </p>
            <p>
                <a class="btn btn-primary btn-lg" href="https://github.com/amad9720/openproject">
                    Voir le Projet Git »
                </a>
            </p>
        </div>

        <div class="row">
            <div id="menu" class="col-md-3">
                <h3></h3>
                <ul class="nav nav-pills nav-stacked">
                    <li><a href="{{ path('hlt_manageur_homepage') }}">Accueil</a></li>
                    <li><a href="{{ path('hlt_manageur_add_meal') }}">Ajouter un repas</a></li>
                    <li><a href="{{ path('hlt_manageur_add_product') }}">Ajouter un produit</a></li>
                </ul>

                <h4>Derniers Ajouts</h4>
                {{ render(controller("HltManageurBundle:Manageur:menu", {'limit': 3})) }}
            </div>
            <div id="content" class="col-md-9">
                {% block inside_body %}
                {% endblock %}
            </div>
        </div>

        <hr>

        <footer>
            <p>BoufMania © {{ 'now'|date('Y') }} All rights reserved.</p>
        </footer>
    </div>
{% endblock %}
